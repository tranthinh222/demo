node{
  // Jenkinsfile - scripted pipeline
  checkout scm
  stages("Build"){
    echo "Building......."
  }
  stages("Build"){
    echo "Building......."
  }
  stages("Build"){
    echo "Building......."
  }
}
